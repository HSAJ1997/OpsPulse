{
  "name": "opspulse",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev -w @opspulse/web",
    "dev:api": "npm run dev -w @opspulse/api"
  }
}